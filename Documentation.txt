1. Execute the required sql scripts.
 
 CREATE SEQUENCE USERDATA_SEQ
 START WITH     1
 INCREMENT BY   1
 NOCACHE
 NOCYCLE;
 
CREATE TABLE USERDATA(USERDATA_ID NUMBER(19,2) PRIMARY KEY, 
USER_NAME VARCHAR2(50), ADDRESS VARCHAR2(50), 
EMAIL_ID VARCHAR2(50), MOBILE_NUMBER NUMBER(19,2), 
PASSWORD VARCHAR2(50));

2. Run the application
3. Use ==> http://localhost:8086/